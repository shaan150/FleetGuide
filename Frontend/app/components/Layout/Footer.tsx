import React from 'react';
import styles from '../../styles/Footer.module.css';

const Footer = () => {
  return (
    <footer className={styles.footer}>
      <small>&copy; Fleet Guide. All rights reserved.</small>
    </footer>
  );
};

export default Footer;
